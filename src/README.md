`profile.json` is the source of truth for generated public website content and generated PDFs.

The `cv_*.de.md` files in this directory are legacy drafts only. They are not read by `scripts/build.mjs`, `scripts/export_pdf.mjs`, or `scripts/export_docx.mjs`.
